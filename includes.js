let numArray = [1, 2, 3, 4, 5];

console.log(numArray.indexOf(0)) // -1 It means it has no this value
console.log(numArray.indexOf(2)) // 1



// includes

console.log(numArray.includes(0)) // false
console.log(numArray.indexOf(2)) // true